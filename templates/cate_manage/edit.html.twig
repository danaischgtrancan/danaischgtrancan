{% extends 'admin.html.twig' %}
{% block stylesheets %}
	<link href="{{asset('css/admin.css')}}" rel="stylesheet" media="all">
{% endblock %}

{% block body %}
	<section class="header mx-5">
		<div class="container mt-4 mb-3">
			<div class="text-center lh-1 mb-2">
				<h4 class="fw-bold">Edit Category</h4>
			</div>
			<div class="d-flex justify-content-end">
				<span>Branch: DANA_ISCHG</span>
			</div>
		</div>

		<div class="container-boder">
			{{ form_start(formCate) }}
			<div class="col-md-12">
				<div class="row">
					<div class="row">
						<div class="col-md-6">
							<div class="forms-inputs mt-3">
								{{ form_label(formCate.name) }}
								{{ form_widget(formCate.name, {'attr':{'autocomplete': 'off'}}) }}
							</div>
						</div>

						<div class="col-md-6">
							<div class="forms-inputs mt-3">
								{{ form_label(formCate.descriptions) }}
								{{ form_widget(formCate.descriptions, {'attr':{'autocomplete': 'off'}}) }}
							</div>
						</div>

						<div class="col-md-12">
							<div class="add mt-3">
								{{form_row(formCate.save, {'attr':{'class': 'btn btn-dark me-4'}})}}
							</div>
						</div>

						{{ form_end(formCate) }}
					</div>
				</div>
			</div>
		</div>
	</tbody>
</section>{% endblock %}
